# GitHub Pages 部署设置指南

本指南将帮助你设置 GitHub Pages 部署，以便在线查看前端页面效果。

## 🚀 快速设置步骤

### 1. 启用 GitHub Pages

1. 访问你的 GitHub 仓库：https://github.com/blackvccat/nekovccat_web
2. 点击仓库顶部的 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**（页面）
4. 在 "Source"（源）部分：
   - 选择 **Deploy from a branch**（从分支部署）
   - Branch（分支）选择：**gh-pages** 或 **main**
   - Folder（文件夹）选择：**/ (root)**
   - 点击 **Save**（保存）

### 2. 配置 GitHub Actions（推荐方式）

如果你想要使用 GitHub Actions 自动部署：

1. 在 **Settings** → **Pages** 中
2. 将 Source 改为：**GitHub Actions**
3. 保存设置

### 3. 触发部署

部署会在以下情况自动触发：

- ✅ 推送到 `main` 分支（当 `frontend/` 目录有变更时）
- ✅ 推送到 `gh-pages` 分支
- ✅ 手动触发：进入 **Actions** 标签页 → 选择工作流 → **Run workflow**

### 4. 查看部署状态

1. 进入 **Actions** 标签页
2. 查看 "Deploy Frontend to GitHub Pages" 工作流
3. 等待构建和部署完成（通常需要 2-5 分钟）

### 5. 访问部署的网站

部署完成后，你的网站将在以下地址可用：

**GitHub Pages URL**: `https://blackvccat.github.io/nekovccat_web`

> ⚠️ **注意**: 首次部署可能需要几分钟时间，GitHub 需要生成 SSL 证书。

## 📋 分支说明

### main 分支
- 主开发分支
- 推送到此分支会自动触发部署（如果 frontend 目录有变更）

### gh-pages 分支
- 专门用于 GitHub Pages 部署的分支
- 可以独立于 main 分支进行部署

## 🔧 配置环境变量（可选）

如果需要自定义后端 API 地址：

1. 进入 **Settings** → **Secrets and variables** → **Actions**
2. 点击 **New repository secret**
3. 添加以下密钥：
   - **Name**: `NEXT_PUBLIC_API_URL`
   - **Value**: 你的后端 API 地址（例如：`https://nekovccat.origin.kim`）

## 🐛 故障排除

### 部署失败

1. **检查 Actions 日志**
   - 进入 **Actions** 标签页
   - 点击失败的工作流运行
   - 查看错误信息

2. **常见问题**：
   - Node.js 版本不兼容 → 已配置为 Node.js 20
   - 依赖安装失败 → 检查 `package-lock.json` 是否存在
   - 构建错误 → 检查 Next.js 配置

### 页面无法访问

1. **检查 GitHub Pages 设置**
   - 确认已启用 GitHub Pages
   - 确认 Source 设置正确

2. **检查部署状态**
   - 进入 **Actions** 标签页
   - 确认部署工作流已成功完成

3. **等待 SSL 证书**
   - 首次部署可能需要几分钟生成 SSL 证书
   - 如果使用自定义域名，需要配置 DNS

### API 请求失败

1. **检查后端服务器**
   - 确认后端 API 服务器正在运行
   - 确认 CORS 设置允许来自 GitHub Pages 域名的请求

2. **检查环境变量**
   - 确认 `NEXT_PUBLIC_API_URL` 已正确配置
   - 默认值：`https://nekovccat.origin.kim`

## 📝 手动部署

如果需要手动触发部署：

```bash
# 1. 确保代码已提交
git add .
git commit -m "Update frontend"

# 2. 推送到 main 分支（会自动触发部署）
git push origin main

# 或者推送到 gh-pages 分支
git checkout gh-pages
git merge main
git push origin gh-pages
```

## 🔄 更新部署

每次更新前端代码后：

1. 提交更改到 Git
2. 推送到 GitHub
3. GitHub Actions 会自动构建和部署
4. 等待几分钟后访问网站查看更新

## 📚 相关文档

- [GitHub Pages 官方文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
- [Next.js 静态导出文档](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)
- [详细部署指南](./frontend/docs/github-pages-deployment.md)

## ✅ 检查清单

- [ ] GitHub Pages 已启用
- [ ] Source 设置为 "GitHub Actions" 或 "Deploy from a branch"
- [ ] 代码已推送到 GitHub
- [ ] GitHub Actions 工作流已运行
- [ ] 部署状态为成功
- [ ] 可以访问部署的网站

---

**部署完成后，访问**: https://blackvccat.github.io/nekovccat_web

